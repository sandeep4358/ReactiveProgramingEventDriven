package com.javafruit.SpringReactiveProgramming;

import org.junit.jupiter.api.Test;
import reactor.core.publisher.Flux;
import reactor.core.publisher.Mono;

public class MonoFluxTest {
//    @Test
//    public void testMono(){
//        Mono<?> stringMono = Mono.just("javafruit")
//                .then(Mono.error(new RuntimeException("Test exception..")))
//                .log();  //Publisher
//        stringMono.subscribe(System.out::println, (e)->System.out.println(e.getMessage()));
//    }
//
//    @Test
//    public void testFlux(){
//        Flux<?> stringFlux = Flux.just("Spring", "Spring Boot", "Spring Reactive", "Kafka event")
//                .concatWithValues("Google").concatWith(Flux.error(new RuntimeException("Exception occure"))).log();
//        stringFlux.subscribe(System.out::println);
//
//    }
}
